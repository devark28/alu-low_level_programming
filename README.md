# C programming

## Modules

- **hello_world** - Compilation stages and basic output

## Usage

```bash
gcc filename.c -o output
```

You can include extra flags for common warnings and errors

```bash
gcc -Wall -Werror -Wextra -pedantic filename.c -o output
```